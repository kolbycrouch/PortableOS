${FETCHCMD} https://ftp.gnu.org/gnu/findutils/findutils-4.6.0.tar.gz
SRCDIR=findutils-4.6.0
${RMCMD} $SRCDIR
${TARCMD} -vxf *.tar.gz
../../patcher
cd $SRCDIR
${MKDIRCMD} build
cd build
FORCE_UNSAFE_CONFIGURE=1 ../configure --prefix=${FINPREFIX} --host=$MACHARCH \
	 --target=$MACHARCH --build=$MACHARCH &&
${MKCMD} &&
${MKCMD} install &&
${MKCMD} clean
