${FETCHCMD} https://ftp.gnu.org/gnu/gmp/gmp-6.1.2.tar.xz
SRCDIR=gmp-6.1.2
${RMCMD} $SRCDIR
${TARCMD} -vxf *.tar.xz
../patcher
cd $SRCDIR
${MKDIRCMD} build
cd build
../configure --prefix=${FINPREFIX} --host=$HOSTARCH --build=$HOSTARCH &&
${MKCMD} &&
${MKCMD} install 
